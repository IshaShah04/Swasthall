<?xml version="1.0" encoding="utf-8"?>
<!--
  Restricts cleartext (HTTP) traffic.
  HTTPS is enforced for all domains EXCEPT localhost (used by dev/emulator tools).
  Supabase, Zego, and FDA API all use HTTPS — no exceptions needed.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Allow cleartext for local development/emulator only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
</network-security-config>
