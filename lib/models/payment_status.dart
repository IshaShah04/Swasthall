// payment_status.dart
// Single source of truth for PaymentStatus enum.
// IMPORTANT: Delete the duplicate `enum PaymentStatus` and `MockPaymentService`
// class from consultation_payment_screen.dart — keep only this file.
enum PaymentStatus { initial, processing, success, failure }
